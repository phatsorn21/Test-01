// src/__ tests __/App.test.tsx

import '@testing-library/jest-dom'
import sum from '../utils/fnSum'



test('demo', () => {
    expect(true).toBe(true)
})

test("add 1 + 2", () => {
    expect(sum(1,2)).toBe(3)
})
test("add 1 + 2", () => {
    expect(sum(1,2)).toBe(3)
})


test('demo', () => {
    expect(true).toBe(true)
})
